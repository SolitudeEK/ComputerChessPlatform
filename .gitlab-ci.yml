stages:
    - test
    - inspect
    - deploydocker
test:
    stage: test
    script:
        - Write-Host "Hello, World!"

inspect:
  stage: inspect
  script:
    - dotnet tool install -g JetBrains.ReSharper.GlobalTools
    - dotnet tool list -g
    - InspectCode --no-swea --output=/resharper-results.xml EngineManagement.sln
  artifacts:
    paths:
      - reports/

deploydocker: 
    stage: deploydocker
    script:
        - cd EngineManagement
        - ./build_services.bat
